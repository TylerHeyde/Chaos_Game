# TO-DO'S
test the make file -- done, it works : )
code the user-program interaction -- included in hermler's file; got it running, implemented the DLLs, changed the color of the rectangles that track mouse movement, the back end stuff needs to be done now
implement text display -- couldn't figure it out, i did add the .tff into the x64 file if someone can attempt it
responsiblities for david -- get the vector running; tracking function already works; any questions, you can text me
-- amrit 4/24
